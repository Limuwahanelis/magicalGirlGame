1. Create camera for a minimap.
2. Set render texture as target fopr camera.
3. Set mask for minimap sprite.
4. Set culling mask for camera
5. (Optional) attach sprite or object to object which sould be displayed 
    on minimap and set itsLayer to the same as culling mask for camera
